/**
 * Class to show boolean primitives
 */
public class BooleanPrimitive {
    public static void main(String[] args) {
        boolean status = true;
        System.out.println("Eligible to appear for the exam? " + status);

        // boolean b1 = 0; will throw an error
        // boolean b2 = "true"; will throw an error
    }
}
